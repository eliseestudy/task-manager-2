services:
  app:
    image: ${APP_IMAGE:-task-manager-2-app:local}
    build: .
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: "file:/app/data/tasks.db"
    volumes:
      - app_data:/app/data

volumes:
  app_data:
